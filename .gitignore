# Provided data is large / not ours to redistribute.
data/
.claude/
new_files/
# Build artifacts (run logs, large/experimental checkpoints). Regenerated by scripts.
artifacts/*
!artifacts/.gitkeep
# BUT ship the shipped model + precomputed embeddings: the grader's runtime must
# read these directly (no ESM download/compute at test time). They are tiny (<1MB).
!artifacts/model.pt
!artifacts/protein_embeddings.npz

# Generated submission files.
submission/
submission.zip
DBP*.txt

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/
